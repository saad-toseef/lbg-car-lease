@page
@model IndexModel
@{
    ViewData["Title"] = "Car";
}


<div class="row">
    <div class="col-lg-1">
        @if (Model.IsLoggedIn)
        {
            <h1>Welcome Back @Model.Username</h1>
        }
        else
        {
            <h1 class="display-4">Welcome</h1>
        }
    </div>
</div>

</>